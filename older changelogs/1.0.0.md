# BLOA 1.0.0-RC1

## Highlights

- Release candidate `1.0.0-RC1`
- All optional features enabled by default (cURL, SQLite, MySQL)
- Enhanced math library with additional functions: factorial, gcd, lcm, is_prime
- New utility functions: array_sum, array_product, string_format
- Improved stability and feature completeness

## Feature list

- Core language features: variables, arithmetic, string interpolation, comparisons, boolean logic, expressions
- Control flow: `if`/`else`, `while`, `for`/`foreach`, `repeat`, `break`, `continue`, `try`/`except`
- Functions and methods with argument handling and return values
- Classes, inheritance, and object creation using `new`
- Module loading with `use` and `require`
- Standard library utilities: `print`, `say`, `ask`, `range`, `len`, `str`, `int`, `float`, `append`, `math`, `io`, `string`, file and directory helpers
- BAAR archive helpers: `baar_create`, `baar_extract`, `baar_list`, `baar_read`
- `.baar` execution semantics: resolves `main.bloa`, `index.bloa`, or first `.bloa` entry when run directly
- Builtin helpers for `isset` and `unset`
- Added compatibility for semicolon-terminated statements outside of string literals

## Notes

- This release candidate captures the current BLOA feature set and prepares the runtime for wider package-based workflows.
- Fixed GitHub Actions release workflow to install Linux build dependencies for cURL and SQLite.
- Fixed Windows build workflow to disable optional cURL/SQLite when native Windows development libraries are unavailable.
- Fixed Linux cross-build workflow for i386/arm targets to disable optional cURL/SQLite and avoid host library mismatches.
- Fixed Debian/Termux package metadata generation so tagged releases like `v1.0.0-RC1` produce valid `Version` fields.
- Termux packages now use Android NDK cross-compilation for proper arm and arm64 Android binaries instead of packaging native Linux executables.
- Existing `.baar` archives can be used as deployable BLOA applications with a single execution command.
- The changelog is stored in `older changelogs/1.0.0.md` for reference while the RC1 candidate is finalized.

---

# BLOA 1.0.0-RC2

## Highlights

- Release candidate `1.0.0-RC2`
- All optional features enabled by default (cURL, SQLite, MySQL)
- Enhanced math library with additional functions: factorial, gcd, lcm, is_prime
- New utility functions: array_sum, array_product, string_format
- Improved stability and feature completeness

## Feature list

- Core language features: variables, arithmetic, string interpolation, comparisons, boolean logic, expressions
- Control flow: `if`/`else`, `while`, `for`/`foreach`, `repeat`, `break`, `continue`, `try`/`except`
- Functions and methods with argument handling and return values
- Classes, inheritance, and object creation using `new`
- Module loading with `use` and `require`
- Standard library utilities: `print`, `say`, `ask`, `range`, `len`, `str`, `int`, `float`, `append`, `math`, `io`, `string`, file and directory helpers
- Math functions: `sqrt`, `pow`, `sin`, `cos`, `tan`, `asin`, `acos`, `atan`, `atan2`, `sinh`, `cosh`, `tanh`, `log`, `log10`, `exp`, `abs`, `floor`, `ceil`, `round`, `fmod`, `degrees`, `radians`, `factorial`, `gcd`, `lcm`, `is_prime`, `pi`, `e`
- Array utilities: `array_sum`, `array_product`, `array_reverse`, `array_sort`, `array_unique`, `array_slice`, `array_join`, `array_split`
- String functions: `split`, `join`, `substr`, `find`, `replace`, `to_upper`, `to_lower`, `trim`, `starts_with`, `ends_with`, `contains`, `reverse`, `repeat`, `capitalize`, `title`, `swapcase`, `zfill`, `lstrip`, `rstrip`, `isalnum`, `isalpha`, `isdigit`, `islower`, `isupper`, `isspace`, `istitle`
- String formatting: `string_format`
- System functions: `getenv`, `setenv`, `system`, `exit`, `getcwd`, `chdir`
- BAAR archive helpers: `baar_create`, `baar_extract`, `baar_list`, `baar_read`
- `.baar` execution semantics: resolves `main.bloa`, `index.bloa`, or first `.bloa` entry when run directly
- Builtin helpers for `isset` and `unset`
- Added compatibility for semicolon-terminated statements outside of string literals
- cURL builtins for HTTP: `curl_get`, `curl_post`, `curl_request`
- SQLite builtins for local database access: `sqlite_query`, `sqlite_exec`
- MySQL builtins for database access: `mysql_connect`, `mysql_query`, `mysql_exec`, `mysql_close`, `mysql_escape`
- JSON/CSV utilities: `json_parse`, `json_stringify`, `csv_parse`, `csv_stringify`
- Filesystem helpers: `path_is_absolute`, `path_normalize`, `file_ext`, `dirname`, `basename`
- Utility functions: `base64_encode`, `base64_decode`, `regex_match`, `regex_replace`, `uuid4`, `glob`, `random_int`, `random_float`, `now`

## Notes

- This release candidate enables all optional features by default for maximum compatibility.
- Math library now includes comprehensive mathematical and number theory functions.
- New array and string utilities enhance data manipulation capabilities.
- All database backends (SQLite, MySQL) and networking (cURL) are now enabled.
